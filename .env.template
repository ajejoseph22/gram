# Workspace Environment Guide
#
# API variables live in: src/api/.env
# Web variables live in: src/web/.env
#
# Copy the package-level examples instead of this file when setting up locally:
#   cp src/api/.env.example src/api/.env
#   cp src/web/.env.example src/web/.env
